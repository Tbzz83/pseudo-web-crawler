[package]
name = "pseudo-web-crawler"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
tokio = {version = "1.50.0", features = ["full"]}

[dev-dependencies]
criterion = "0.5"


[[bench]]
name = "url_frontier_benchmark"
harness = false
